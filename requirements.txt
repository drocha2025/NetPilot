Flask==3.1.3
# Provides the Flask web framework used by the NetPilot Unified NOC dashboard.

netmiko==4.7.0
# Provides SSH connectivity to the Cisco routers used by NetPilot.

paramiko==3.5.1
# Pins Paramiko to a version compatible with Netmiko 4.7.0 and avoids the vulnerable 4.0.0 release.

psycopg[binary]==3.3.5
# Provides PostgreSQL connectivity for NetPilot.

requests==2.34.2
# Provides HTTP communication used by NetPilot, including communication with Ollama.

python-dotenv
# Provides optional support for loading environment variables from a .env file.